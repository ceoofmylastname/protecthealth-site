{
  "name": "protecthealth-site",
  "type": "module",
  "version": "1.0.0",
  "description": "ProtectHealth 2.0, AEO/GEO-optimized insurance website. Strategy over product.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/sitemap-lastmod.mjs && node scripts/llms-txt.mjs && node scripts/indexnow-ping.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.2.0",
    "@supabase/supabase-js": "^2.110.8",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "sharp": "^0.35.3"
  }
}

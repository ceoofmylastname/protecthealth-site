---
import Base from '../layouts/Base.astro';
import ReviewBadge from '../components/ReviewBadge.astro';
import { SITE, ASSETS, HF_ASSETS, TEAM, BLOG_ART, breadcrumbSchema, speakableSchema } from '../lib/site';
import { SERVICES } from '../lib/services';
import { GOOGLE_REVIEWS } from '../lib/reviews';
import { getCollection } from 'astro:content';

const reviewRowA = GOOGLE_REVIEWS.slice(0, 10);
const reviewRowB = GOOGLE_REVIEWS.slice(10);

// Cinematic intro: plays once per browser session on the homepage, then dissolves into the page.
const INTRO_VIDEO = 'https://d8j0ntlcm91z4.cloudfront.net/user_3GY61bj0wPgc3RYJDtgyJ6LmhTH/hf_20260722_062759_b0f2c41b-4f29-4be1-bdf5-f542bfd663af.mp4';

const posts = (await getCollection('blog'))
  .sort((a, b) => b.data.datePublished.localeCompare(a.data.datePublished))
  .slice(0, 3);

const schemas = [
  speakableSchema(SITE.domain),
  breadcrumbSchema([{ name: 'Home', url: `${SITE.domain}/` }]),
];

// Inline SVG icon set — always renders, no network needed.
const ICONS: Record<string, string> = {
  'health-insurance': '<svg viewBox="0 0 48 48" fill="none"><path d="M24 41S7 30.6 7 18.5C7 12.7 11.6 8 17.4 8c3 0 5.7 1.3 6.6 3.3C24.9 9.3 27.6 8 30.6 8 36.4 8 41 12.7 41 18.5 41 30.6 24 41 24 41Z" stroke="url(#g)" stroke-width="2.6" stroke-linejoin="round"/><path d="M24 17v12M18 23h12" stroke="#19c8ff" stroke-width="2.6" stroke-linecap="round"/></svg>',
  'life-insurance': '<svg viewBox="0 0 48 48" fill="none"><path d="M6 24C6 14 14 7 24 7s18 7 18 17" stroke="url(#g)" stroke-width="2.6" stroke-linecap="round"/><path d="M6 24c3-3 6-3 9 0 3-3 6-3 9 0 3-3 6-3 9 0 3-3 6-3 9 0" stroke="url(#g)" stroke-width="2.6" stroke-linecap="round"/><path d="M24 24v12a5 5 0 0 0 10 0" stroke="#19c8ff" stroke-width="2.6" stroke-linecap="round"/></svg>',
  'gap-health-insurance': '<svg viewBox="0 0 48 48" fill="none"><rect x="5" y="16" width="14" height="16" rx="3.5" stroke="url(#g)" stroke-width="2.6"/><rect x="29" y="16" width="14" height="16" rx="3.5" stroke="url(#g)" stroke-width="2.6"/><path d="M19 24h10" stroke="#19c8ff" stroke-width="2.6" stroke-linecap="round" stroke-dasharray="3 4"/></svg>',
  'medicare': '<svg viewBox="0 0 48 48" fill="none"><path d="M24 5 40 11v11c0 10-6.7 17.4-16 21C14.7 39.4 8 32 8 22V11L24 5Z" stroke="url(#g)" stroke-width="2.6" stroke-linejoin="round"/><path d="M24 16v12M18 22h12" stroke="#19c8ff" stroke-width="2.6" stroke-linecap="round"/></svg>',
  'dental-insurance': '<svg viewBox="0 0 48 48" fill="none"><path d="M15 7c3 0 5.5 2 9 2s6-2 9-2c5 0 8 4 8 9 0 9-4 14-6 24-.5 2.4-3.6 2.6-4.4.3L27.5 30c-1-3-6-3-7 0l-3.1 10.3c-.8 2.3-3.9 2.1-4.4-.3C11 30 7 25 7 16c0-5 3-9 8-9Z" stroke="url(#g)" stroke-width="2.6" stroke-linejoin="round"/><path d="m33 13 1.2 2.6L37 17l-2.8 1.4L33 21l-1.2-2.6L29 17l2.8-1.4L33 13Z" fill="#19c8ff"/></svg>',
  'vision-insurance': '<svg viewBox="0 0 48 48" fill="none"><path d="M4 24S12 12 24 12s20 12 20 12-8 12-20 12S4 24 4 24Z" stroke="url(#g)" stroke-width="2.6" stroke-linejoin="round"/><circle cx="24" cy="24" r="6" stroke="#19c8ff" stroke-width="2.6"/></svg>',
};

// Real Google reviewer faces for the hero social-proof strip (photos from the live listing).
const HERO_REVIEWERS = ['Yvette Auger', 'Stephen Kuo', 'Rachel Wren', 'Yudith Felix']
  .map((n) => GOOGLE_REVIEWS.find((r) => r.name === n))
  .filter((r) => r && r.photo);
---
<Base
  title="Insurance Strategy For Everyone | ProtectHealth Las Vegas"
  description="ProtectHealth is a Las Vegas insurance brokerage that builds coverage around your life and business — health, life, dental, vision, Medicare, and employer benefits. Strategy first, product second."
  schemas={schemas}
>
  <!-- ============ CINEMATIC INTRO (plays once per session, dissolves into homepage) ============ -->
  <div id="site-intro" aria-hidden="true">
    <video id="intro-video" data-src={INTRO_VIDEO} muted playsinline preload="none"></video>
    <div class="intro-brand">P R O T E C T H E A L T H</div>
    <button id="intro-skip" type="button">Skip Intro →</button>
  </div>

  <!-- shared SVG gradient defs -->
  <svg width="0" height="0" style="position:absolute" aria-hidden="true"><defs><linearGradient id="g" x1="0" y1="0" x2="48" y2="48"><stop stop-color="#197bff"/><stop offset="1" stop-color="#007db3"/></linearGradient></defs></svg>

  <!-- ============ HERO ============ -->
  <div id="scrollbar" aria-hidden="true"><i id="scrollbar-fill"></i></div>
  <section class="lux-hero" style="background-image:url('/assets/art-hero.webp')">
    <div class="hero-bg" id="heroBg" style={`background-image:url('${HF_ASSETS.hero}')`}></div>
    <div class="hero-duotone"></div>
    <div class="aurora"></div>
    <div class="mesh m1"></div><div class="mesh m2"></div><div class="mesh m3"></div>
    <div class="grain"></div>
    <div class="container lux-hero-grid">
      <div class="lux-hero-copy">
        <div class="kicker-chip reveal"><i class="kdot"></i> Strategy-First Insurance · Las Vegas</div>
        <h1 class="headline">
          <span class="hw">Strategy</span> <span class="hw hl-under">first.<svg viewBox="0 0 180 14" preserveAspectRatio="none" aria-hidden="true"><path d="M4 10 C 50 3, 130 3, 176 8" fill="none" stroke="url(#hu)" stroke-width="6" stroke-linecap="round"/><defs><linearGradient id="hu" x1="0" y1="0" x2="180" y2="0" gradientUnits="userSpaceOnUse"><stop stop-color="#19c8ff"/><stop offset="1" stop-color="#66e0ff"/></linearGradient></defs></svg></span><br/>
          <span class="hw grad-anim">Insurance</span> <span class="hw grad-anim">second.</span>
        </h1>
        <p class="lead reveal d3">The rest of the industry starts with a product. <b style="color:#fff;">ProtectHealth starts with how your life and business are actually built.</b></p>
        <div class="hero-ctas reveal d4">
          <a href="/contact-us" class="btn big glow">Talk To A Broker</a>
          <a href="/free-quote" class="btn big glass no-arrow">Get Started</a>
        </div>
        <div class="avatars-strip reveal d5">
          <div class="avatars-init">
            {HERO_REVIEWERS.map((r) => (
              <span title={`${r.name} · Google review`}>
                {r.name.split(' ').map((w) => w[0]).join('').slice(0, 2)}
                <img src={r.photo} alt={`${r.name}, Google reviewer`} loading="lazy" referrerpolicy="no-referrer" onerror="this.remove()" />
              </span>
            ))}
          </div>
          <div class="txt"><b><span class="stars">★★★★★</span> <span data-count="4.9" data-dec="1">4.9</span> rating</b>Trusted by Nevadans across Clark County</div>
        </div>
      </div>

      <!-- glass strategy card (self-contained, photo enhances behind) -->
      <div class="hero-panel reveal d3">
        <div class="hero-photo" style={`background-image:url('${HF_ASSETS.hero}')`}></div>
        <div class="glass-card spot tilt">
          <div class="gc-row"><span class="stars">★★★★★</span><b>4.9</b><span class="gc-muted">· 703+ Google reviews</span></div>
          <div class="gc-title">Your coverage strategy</div>
          <ul class="gc-list">
            <li><i>✓</i> Network checked against your doctors</li>
            <li><i>✓</i> Tax-credit eligibility mapped</li>
            <li><i>✓</i> Business structure reviewed</li>
            <li><i>✓</i> Total exposure, not just premium</li>
          </ul>
          <div class="gc-foot">Strategy conversation · 20 min · $0</div>
        </div>
        <div class="float-pill p2">✓ Official Paychex Partner</div>
      </div>
    </div>
  </section>

  <!-- ============ MARQUEE ============ -->
  <div class="marquee" aria-hidden="true">
    <div class="marquee-track">
      {[1,2].map(() => (
        <span>Strategy over product <i>✦</i> 4.9 stars · 703+ Google reviews <i>✦</i> Las Vegas · Nevada <i>✦</i> Official Paychex Partner <i>✦</i> Health · Life · Medicare · Dental · Vision <i>✦</i> Insurance nerds, not tax professionals <i>✦</i> </span>
      ))}
    </div>
  </div>

  <!-- ============ STATS ============ -->
  <section class="section grad-soft" style="padding-bottom:34px;">
    <div class="container">
      <div class="stat-row">
        <div class="stat spot tilt glassy reveal"><div class="num"><span data-count="4.9" data-dec="1">0</span></div><div class="lbl">Google rating</div><i class="tick"></i></div>
        <div class="stat spot tilt glassy reveal d1"><div class="num"><span data-count="703">0</span>+</div><div class="lbl">Five-star reviews</div><i class="tick"></i></div>
        <div class="stat spot tilt glassy reveal d2"><div class="num"><span data-count="6">0</span></div><div class="lbl">Coverage lines</div><i class="tick"></i></div>
        <div class="stat spot tilt glassy reveal d3"><div class="num">$<span data-count="0">0</span></div><div class="lbl">Cost to talk to us</div><i class="tick"></i></div>
      </div>
      <div class="ai-wrap spot reveal">
        <div class="ai-label">The ProtectHealth Difference</div>
        <div id="ai-summary">
          ProtectHealth is a Las Vegas, Nevada insurance brokerage serving individuals, families, self-employed professionals, and small businesses. ProtectHealth brokers compare plans across carriers for health, life, GAP, Medicare, dental, and vision coverage, and help business owners explore employer benefits alongside payroll and HR support through an official Paychex partnership. Consultations are free and start with strategy, not a sales pitch.
        </div>
      </div>
    </div>
  </section>

  <!-- ============ SERVICES ============ -->
  <!-- ============ COVERAGE — sticky stacking deck ============ -->
  <section class="stackzone" id="coverage">
    <div class="sz-gridbg" aria-hidden="true"></div>
    <div class="sz-glow" aria-hidden="true"></div>
    <div class="container sz-cols">
      <div class="sz-left">
        <div class="sz-pinhead">
          <div class="section-label" style="background:linear-gradient(90deg,#66e0ff,#8ec9ff);-webkit-background-clip:text;background-clip:text;">Our Portfolio</div>
          <h2>Six lines of coverage.<br /><span class="hs-grad">One strategy.</span></h2>
          <p>Whether you're covering a family, a business, or just yourself, the process starts with you, not a plan brochure. Keep scrolling and watch the deck stack up.</p>
          <a href="/buyers-guide" class="btn big glass no-arrow" style="margin-top:8px;">Read The Buyer's Guide</a>
        </div>
      </div>
      <div class="sz-right">
        {SERVICES.map((s, i) => (
          <figure class="sz-pin">
            <a class={`sz-card r${i % 3}`} href={`/services/${s.slug}`} style={`--i:${i}`}>
              <div class="sz-top">
                <div class="icon-squircle" set:html={ICONS[s.slug]} />
                <span class="sz-idx">0{i + 1}</span>
              </div>
              <h3>{s.name}</h3>
              <p>{s.description}</p>
              <span class="sz-more">Learn More →</span>
            </a>
          </figure>
        ))}
      </div>
    </div>
  </section>

  <!-- ============ WHY / ADVISOR ============ -->
  <section class="section">
    <div class="container">
      <div class="split-feature">
        <div class="why-panel reveal" style="background-image:url('/assets/art-light.webp')">
          <div class="why-photo" style={`background-image:url('${HF_ASSETS.advisor}')`}></div>
          <div class="chip c1"><b>Networks</b> checked first</div>
          <div class="chip c2"><b>Tax credits</b> mapped</div>
          <div class="chip c3"><b>$0</b> consultation</div>
        </div>
        <div class="reveal d1">
          <div class="section-label">Why have insurance?</div>
          <h2 style="margin-top:0;">You can't buy it <span class="grad-text">after</span> you need it.</h2>
          <p>You can't buy auto insurance after someone crashes into you, and you can't buy health insurance after you get sick or injured. Coverage only works when it's in place before the bad day.</p>
          <ul class="checklist">
            <li>Built around age, income &amp; tax-credit eligibility</li>
            <li>Networks checked against your actual doctors</li>
            <li>Prescriptions priced before you enroll</li>
            <li>Total financial exposure — not just the premium</li>
          </ul>
          <a href="/about-us" class="btn" style="margin-top:16px;">Our Approach</a>
        </div>
      </div>
    </div>
  </section>

  <!-- ============ CAMPAIGN DUO ============ -->
  <section class="section" style="padding-top:20px;">
    <div class="container">
      <div class="grid cols-2">
        <a class="photo-card spot tilt reveal" href="/self-employed" style="background-image:url('/assets/art-realtor.webp')">
          <div class="pc-photo" style={`background-image:url('${HF_ASSETS.realtor}')`}></div>
          <div class="pc-overlay"></div>
          <div class="pc-body">
            <span class="pc-tag">Realtors · 1099 · Self-Employed</span>
            <h3>Your coverage should be built around your business.</h3>
            <p>ICHRA, Section 105, genuine PPO access, small-group opportunities — the options nobody ever explained.</p>
            <span class="pc-btn">Explore The Strategy →</span>
          </div>
        </a>
        <a class="photo-card spot tilt reveal d1" href="/employers" style="background-image:url('/assets/art-employers.webp')">
          <div class="pc-photo" style={`background-image:url('${HF_ASSETS.employers}')`}></div>
          <div class="pc-overlay"></div>
          <div class="pc-body">
            <span class="pc-tag">Business Owners · Paychex Partner</span>
            <h3>Your insurance agency just got a much bigger toolbox.</h3>
            <p>Benefits, payroll, HR, compliance, and retirement — one conversation, the whole employer picture.</p>
            <span class="pc-btn">See What That Unlocks →</span>
          </div>
        </a>
      </div>
    </div>
  </section>

  <!-- ============ WHY CHOOSE / NAVY ============ -->
  <section class="section" style="padding-top:20px;">
    <div class="container">
      <div class="navy-band lux" style="background-image:url('/assets/art-cta.webp');background-size:cover;">
        <div class="band-glow"></div>
        <div class="section-label" style="color:#8ec9ff;">Why Choose Us?</div>
        <h2 style="margin-top:0;max-width:560px;">We understand that everyone's insurance needs are unique.</h2>
        <div class="grid cols-2">
          <div class="feature-num reveal"><div class="n">1</div><div><h3>Customized Coverage</h3><p>Strategies built around your household, your business structure, and your real financial exposure.</p></div></div>
          <div class="feature-num reveal d1"><div class="n">2</div><div><h3>Exceptional Customer Service</h3><p>A dedicated team ready to assist with any question — before, during, and after enrollment.</p></div></div>
          <div class="feature-num reveal d2"><div class="n">3</div><div><h3>Trusted Expertise</h3><p>Years of industry experience across health, life, Medicare, and employer benefits.</p></div></div>
          <div class="feature-num reveal d3"><div class="n">4</div><div><h3>Strong Partnerships</h3><p>Transparency, integrity, and an official Paychex partnership that gives business owners a bigger toolbox.</p></div></div>
        </div>
      </div>
    </div>
  </section>

  <!-- ============ REVIEW SHOWCASE ============ -->
  <!-- ============ v8.3: METHOD COVERFLOW CAROUSEL ============ -->
  <section class="section" id="method" style="padding-bottom:30px;">
    <div class="container">
      <div class="mframe reveal">
        <div class="mpanel">
          <div class="mbadge"><span class="mspark">✦</span> The ProtectHealth Method</div>
          <div class="mhead">
            <h2>Four steps. <span class="grad-text">Zero brochures.</span></h2>
            <p>How every ProtectHealth conversation runs, from first hello to years later.</p>
          </div>
          <div class="mcarousel" id="mCarousel">
            <div class="mtrack" id="mTrack">
              {[
                { n: '01', t: 'Listen', img: '/assets/buyers-guide/06-conversation.webp', alt: 'Two glass chairs facing each other under a warm spotlight', p: 'The first 20 minutes are about your life, not our products. No brochure makes an appearance.', href: '/contact-us', link: 'Start the conversation' },
                { n: '02', t: 'Map', img: '/assets/buyers-guide/01-hero.webp', alt: 'Glass compass resting on insurance documents', p: 'Income, tax credits, business structure, real exposure. The map decides what belongs on the table.', href: '/buyers-guide', link: 'See how we think' },
                { n: '03', t: 'Match', img: '/assets/buyers-guide/04-toolbox.webp', alt: 'Glass toolbox with six glowing coverage instruments', p: 'Only now do products enter the room, auditioning for a job that is already defined.', href: '/services', link: 'The coverage lines' },
                { n: '04', t: 'Stay', img: '/assets/buyers-guide/05-timing.webp', alt: 'Glass hourglass beside a calendar of light', p: 'Renewals, network shuffles, life changes. The strategy gets maintained by people who answer the phone.', href: '/our-team', link: 'Meet the team' },
              ].concat([
                { n: '01', t: 'Listen', img: '/assets/buyers-guide/06-conversation.webp', alt: 'Two glass chairs facing each other under a warm spotlight', p: 'The first 20 minutes are about your life, not our products. No brochure makes an appearance.', href: '/contact-us', link: 'Start the conversation' },
                { n: '02', t: 'Map', img: '/assets/buyers-guide/01-hero.webp', alt: 'Glass compass resting on insurance documents', p: 'Income, tax credits, business structure, real exposure. The map decides what belongs on the table.', href: '/buyers-guide', link: 'See how we think' },
                { n: '03', t: 'Match', img: '/assets/buyers-guide/04-toolbox.webp', alt: 'Glass toolbox with six glowing coverage instruments', p: 'Only now do products enter the room, auditioning for a job that is already defined.', href: '/services', link: 'The coverage lines' },
                { n: '04', t: 'Stay', img: '/assets/buyers-guide/05-timing.webp', alt: 'Glass hourglass beside a calendar of light', p: 'Renewals, network shuffles, life changes. The strategy gets maintained by people who answer the phone.', href: '/our-team', link: 'Meet the team' },
              ]).map((st) => (
                <article class="mcard">
                  <div class="mcard-img"><img src={st.img} alt={st.alt} loading="lazy" /></div>
                  <div class="mcard-body">
                    <span class="mnum">{st.n}</span>
                    <h3>{st.t}</h3>
                    <p>{st.p}</p>
                    <a href={st.href} class="mlink">{st.link} →</a>
                  </div>
                </article>
              ))}
            </div>
            <button class="mnav mprev" aria-label="Previous step" type="button">←</button>
            <button class="mnav mnext" aria-label="Next step" type="button">→</button>
            <div class="mdots" id="mDots" role="tablist" aria-label="Method steps"></div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="section" style="padding-top:26px;">
    <div class="container">
      <a class="review-showcase spot reveal" href={SITE.reviews.url} target="_blank" rel="noopener noreferrer">
        <div class="rs-glow"></div>
        <div class="rs-left">
          <div class="rs-g">G</div>
          <div>
            <div class="rs-stars stars">★★★★★</div>
            <div class="rs-big"><span data-count="4.9" data-dec="1">4.9</span> <em>/ 5</em></div>
          </div>
        </div>
        <div class="rs-mid">
          <div class="rs-count"><span data-count="703">703</span>+ Google reviews</div>
          <div class="rs-sub">Insurance agency in Clark County, Nevada — rated by the people who had the conversation.</div>
        </div>
        <div class="rs-cta">Read the reviews →</div>
      </a>
    </div>

    <!-- Real Google reviews wall — verbatim from the live listing, display-only (no review schema by design) -->
    <div class="review-wall reveal">
      {[reviewRowA, reviewRowB].map((row, ri) => (
        <div class={`rv-row ${ri === 1 ? 'rv-reverse' : ''}`}>
          <div class="rv-track">
            {[...row, ...row].map((r) => (
              <a class="rv-card" href={SITE.reviews.url} target="_blank" rel="noopener noreferrer" aria-label={`Google review by ${r.name}`}>
                <div class="rv-head">
                  <span class="rv-avatar">
                    {r.name.charAt(0)}
                    {r.photo && <img src={r.photo} alt={`${r.name} on Google`} loading="lazy" referrerpolicy="no-referrer" onerror="this.remove()" />}
                  </span>
                  <span class="rv-meta"><b>{r.name}</b><em><span class="rv-stars">★★★★★</span> · {r.date}</em></span>
                  <span class="rv-g">G</span>
                </div>
                <p class="rv-text">{r.text}</p>
              </a>
            ))}
          </div>
        </div>
      ))}
      <p class="rv-note">Real reviews, word for word, from ProtectHealth's Google listing. <a href={SITE.reviews.url} target="_blank" rel="noopener noreferrer">Read all {SITE.reviews.count}+ on Google</a>.</p>
    </div>
  </section>

  <!-- ============ TEAM ============ -->
  <section class="section soft rounded-section">
    <div class="container">
      <div class="section-head center">
        <div class="section-label">Our Team</div>
        <h2 style="margin-top:0;">Meet the professionals behind the strategy</h2>
        <p style="color:var(--muted);max-width:560px;margin:10px auto 0;">Six licensed brokers, one philosophy: the product should serve the strategy, not become the strategy.</p>
      </div>
      <div class="team-fan reveal">
        {['jason-vasquez', 'brenda-morgen', 'robert-morgen', 'brian-douglas', 'janet-nevarez', 'chris-bridgeforth']
          .map((slug) => TEAM.find((m) => m.slug === slug))
          .filter(Boolean)
          .map((m, i, arr) => {
          const d = i - (arr.length - 1) / 2;
          return (
            <a
              class="fan-card"
              href={`/team-members/${m.slug}`}
              aria-label={`${m.name}, ${m.role} at ProtectHealth`}
              style={`--fx:${(d * 95).toFixed(0)}px; --fy:${(Math.abs(d) * -26).toFixed(0)}px; --fr:${(d * 10).toFixed(1)}deg; --fd:${i * 90}ms; z-index:${arr.length - Math.round(Math.abs(d) * 2)};`}
            >
              <img src={m.photo} alt={`${m.name}, ${m.role} at ProtectHealth`} loading="lazy" />
              <span class="fan-name"><b>{m.name.split(' ')[0]}</b>{m.role.split('/')[0].trim()}</span>
            </a>
          );
        })}
      </div>
      <div style="text-align:center;margin-top:26px;">
        <a href="/our-team" class="btn">Meet The Whole Team</a>
      </div>
    </div>
  </section>

  <!-- ============ BLOG ============ -->
  <section class="section">
    <div class="container">
      <div class="section-head">
        <div class="section-label">Blog / News</div>
        <h2 style="margin-top:0;">Our latest news &amp; guides</h2>
      </div>
      <div class="b8-list">
        {posts.map((p, i) => (
          <article class={`b8-row reveal d${i}`}>
            <div class="b8-copy">
              <div class="b8-tags">
                <span>{p.data.category}</span>
                {p.data.funnelStage && <span>{p.data.funnelStage === 'TOFU' ? 'Start Here' : p.data.funnelStage === 'MOFU' ? 'Go Deeper' : 'Take Action'}</span>}
              </div>
              <h3><a href={`/blog-post/${p.id}`}>{p.data.title}</a></h3>
              <p>{p.data.description}</p>
              <div class="b8-meta">{p.data.author ?? 'ProtectHealth Team'} <i>•</i> {new Date(p.data.datePublished).toLocaleDateString('en-US', { day: 'numeric', month: 'short', year: 'numeric' })}</div>
              <a class="b8-more" href={`/blog-post/${p.id}`}>Read more <span class="b8-arrow">→</span></a>
            </div>
            <a class="b8-media" href={`/blog-post/${p.id}`} aria-hidden="true" tabindex="-1">
              {BLOG_ART[p.id] && <img src={BLOG_ART[p.id].webp} alt={BLOG_ART[p.id].alt} loading="lazy" />}
            </a>
          </article>
        ))}
      </div>
      <div style="text-align:center;margin-top:34px;">
        <a href="/blog" class="btn ghost no-arrow">Browse All Guides</a>
      </div>
    </div>
  </section>

  <!-- ============ FINAL CTA ============ -->
  <section class="section" style="padding-top:0;">
    <div class="container">
      <div class="cta-band lux reveal" style="background-image:url('/assets/art-cta.webp');background-size:cover;">
        <h2>20 minutes. No pitch. <span style="color:#66e0ff;">Just clarity.</span></h2>
        <p>Bring your current coverage — or your lack of it — and walk away knowing which options fit, which don't, and what questions to ask next. If what you have is right, ProtectHealth will tell you that too.</p>
        <div style="display:flex;gap:14px;flex-wrap:wrap;">
          <a href="/contact-us" class="btn big white">Book A Strategy Conversation</a>
          <a href={SITE.phoneHref} class="btn big glass no-arrow">Call {SITE.phone}</a>
        </div>
      </div>
    </div>
  </section>
</Base>

<style>
  /* ===== hero ===== */
  .lux-hero { position: relative; overflow: hidden; background-color: #0a2246; background-size: cover; background-position: center; color: #fff; padding: 92px 0 100px; border-radius: 34px; margin: 8px 14px 0; }
  .mesh { position: absolute; border-radius: 50%; filter: blur(90px); opacity: 0.45; animation: drift 16s ease-in-out infinite alternate; }
  .m1 { width: 560px; height: 560px; background: #197bff; top: -180px; right: -120px; }
  .m2 { width: 420px; height: 420px; background: #007db3; bottom: -160px; left: -140px; animation-delay: -5s; }
  .m3 { width: 300px; height: 300px; background: #19c8ff; top: 40%; left: 46%; opacity: 0.22; animation-delay: -9s; }
  @keyframes drift { from { transform: translate(0,0) scale(1); } to { transform: translate(46px,-34px) scale(1.12); } }
  .grain { position: absolute; inset: 0; opacity: 0.05; background-image: radial-gradient(rgba(255,255,255,0.7) 0.6px, transparent 0.6px); background-size: 3px 3px; pointer-events: none; }
  .lux-hero-grid { position: relative; z-index: 2; display: grid; grid-template-columns: 1.08fr 0.92fr; gap: 56px; align-items: center; }
  .kicker-chip { display: inline-flex; align-items: center; gap: 9px; text-transform: uppercase; letter-spacing: 2.6px; font-size: 0.74rem; font-weight: 800; color: #bfe2ff; margin-bottom: 20px; padding: 8px 16px; border-radius: 999px; border: 1px solid rgba(142,201,255,0.35); background: linear-gradient(120deg, rgba(25,123,255,0.18), rgba(25,200,255,0.1)); backdrop-filter: blur(6px); }
  .kicker-chip .kdot { width: 7px; height: 7px; border-radius: 50%; background: #19c8ff; box-shadow: 0 0 0 0 rgba(25,200,255,0.7); animation: kpulse 2.2s ease infinite; }
  @keyframes kpulse { 0% { box-shadow: 0 0 0 0 rgba(25,200,255,0.65); } 70% { box-shadow: 0 0 0 9px rgba(25,200,255,0); } 100% { box-shadow: 0 0 0 0 rgba(25,200,255,0); } }
  .lux-hero h1 { color: #fff; font-family: var(--font-display); font-size: clamp(2.7rem, 6.2vw, 4.7rem); line-height: 1.02; letter-spacing: -0.03em; }
  .hl-under { position: relative; }
  .hl-under svg { position: absolute; left: 2%; bottom: -0.12em; width: 96%; height: 0.22em; }
  .hl-under svg path { stroke-dasharray: 260; stroke-dashoffset: 260; animation: hdraw 1s cubic-bezier(.2,.7,.2,1) 0.9s forwards; }
  @keyframes hdraw { to { stroke-dashoffset: 0; } }

  /* animated headline */
  .hw { display: inline-block; opacity: 0; transform: translateY(0.6em) rotate(1.2deg); animation: wordUp 0.7s cubic-bezier(0.2, 0.7, 0.2, 1) forwards; }
  .headline .hw:nth-child(1){animation-delay:.05s}.headline .hw:nth-child(2){animation-delay:.16s}
  .headline .hw:nth-child(4){animation-delay:.34s}.headline .hw:nth-child(5){animation-delay:.46s}
  @keyframes wordUp { to { opacity: 1; transform: none; } }
  .grad-anim { background: linear-gradient(92deg, #8ec9ff, #19c8ff 40%, #66e0ff 60%, #8ec9ff); background-size: 220% 100%; -webkit-background-clip: text; background-clip: text; color: transparent; animation: wordUp 0.7s cubic-bezier(0.2,0.7,0.2,1) forwards, shimmer 5s linear 1.2s infinite; }
  @keyframes shimmer { to { background-position: 220% 0; } }
  .grad-text { background: linear-gradient(92deg, #197bff, #19c8ff 45%, #007db3 65%, #197bff); background-size: 220% 100%; -webkit-background-clip: text; background-clip: text; color: transparent; animation: shimmer 6s linear infinite; }

  .lux-hero .lead { color: #d9e6f7; font-size: 1.16rem; margin: 20px 0 30px; max-width: 560px; }
  .btn.glow { box-shadow: 0 8px 26px rgba(0,125,179,0.45); animation: pulseGlow 2.6s ease-in-out infinite; }
  @keyframes pulseGlow { 0%,100% { box-shadow: 0 8px 26px rgba(0,125,179,0.45); } 50% { box-shadow: 0 8px 42px rgba(25,200,255,0.68); } }

  .avatars-init { display: flex; }
  .avatars-init span { width: 46px; height: 46px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-weight: 800; font-size: 0.8rem; color: #fff; border: 3px solid rgba(255,255,255,0.9); margin-left: -12px; background: linear-gradient(135deg, #197bff, #007db3); box-shadow: 0 6px 16px rgba(0,0,0,0.25); }
  .avatars-init span:first-child { margin-left: 0; }
  .avatars-init span:nth-child(2){ background: linear-gradient(135deg,#0f3567,#197bff); }
  .avatars-init span:nth-child(3){ background: linear-gradient(135deg,#007db3,#19c8ff); }
  .avatars-init span:nth-child(4){ background: linear-gradient(135deg,#17457e,#007db3); }
  .avatars-init span { position: relative; overflow: hidden; }
  .avatars-init span img { position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; border-radius: 50%; }
  .avatars-strip { display: flex; align-items: center; gap: 16px; margin-top: 38px; }
  .avatars-strip .txt { font-size: 0.92rem; color: #d9e6f7; }
  .avatars-strip .txt b { color: #fff; display: block; font-size: 1.05rem; }

  /* hero glass panel */
  .hero-panel { position: relative; min-height: 460px; }
  .hero-photo { position: absolute; inset: 0; border-radius: 30px; background: linear-gradient(135deg, rgba(25,123,255,0.35), rgba(0,125,179,0.25)); background-size: cover; background-position: center; border: 1px solid rgba(255,255,255,0.22); box-shadow: 0 30px 80px rgba(0,0,0,0.45); }
  .glass-card { position: absolute; left: -30px; bottom: 34px; width: min(330px, 86%); background: rgba(255,255,255,0.12); border: 1px solid rgba(255,255,255,0.35); border-radius: 22px; padding: 22px 24px; backdrop-filter: blur(16px); box-shadow: 0 24px 60px rgba(4,10,24,0.45); animation: floaty 6s ease-in-out infinite; }
  .gc-row { display: flex; align-items: center; gap: 8px; font-size: 0.9rem; }
  .gc-row b { font-size: 1.05rem; }
  .gc-muted { color: #cfe0f5; font-size: 0.82rem; }
  .gc-title { font-family: var(--font-display); font-weight: 700; font-size: 1.15rem; margin: 10px 0 8px; }
  .gc-list { list-style: none; margin: 0; }
  .gc-list li { font-size: 0.88rem; color: #e6effb; margin: 7px 0; display: flex; gap: 9px; align-items: center; }
  .gc-list i { font-style: normal; width: 19px; height: 19px; flex: 0 0 19px; border-radius: 50%; background: linear-gradient(135deg,#19c8ff,#007db3); display: flex; align-items: center; justify-content: center; font-size: 0.62rem; font-weight: 800; }
  .gc-foot { margin-top: 14px; padding-top: 12px; border-top: 1px solid rgba(255,255,255,0.22); font-size: 0.78rem; letter-spacing: 0.12em; text-transform: uppercase; color: #8ec9ff; font-weight: 700; }
  .float-pill { position: absolute; background: rgba(255,255,255,0.95); color: var(--navy); font-weight: 800; font-size: 0.85rem; padding: 11px 18px; border-radius: 40px; box-shadow: 0 14px 34px rgba(10,34,70,0.4); animation: floaty 5.5s ease-in-out infinite; }
  .p2 { top: 22px; right: -14px; animation-delay: -2s; }
  @keyframes floaty { 0%,100% { transform: translateY(0); } 50% { transform: translateY(-9px); } }

  /* marquee */
  .marquee { background: #0a2246; color: #8ec9ff; overflow: hidden; padding: 13px 0; border-top: 1px solid rgba(255,255,255,0.08); }
  .marquee-track { display: flex; white-space: nowrap; animation: marquee 30s linear infinite; font-size: 0.82rem; letter-spacing: 0.22em; text-transform: uppercase; font-weight: 700; }
  .marquee-track span { padding-right: 2ch; }
  .marquee-track i { color: #19c8ff; font-style: normal; padding: 0 1ch; }
  @keyframes marquee { from { transform: translateX(0); } to { transform: translateX(-50%); } }

  /* stats */
  .grad-soft { background: linear-gradient(180deg, #eef5fc 0%, #ffffff 60%); }
  .stat-row { display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); gap: 20px; }
  .stat { overflow: hidden; background: linear-gradient(#fff,#fff) padding-box, linear-gradient(120deg, #197bff55, #19c8ff44 50%, #007db377) border-box; border: 1.5px solid transparent; border-radius: 22px; padding: 28px 26px 30px; text-align: center; box-shadow: var(--shadow-sm); }
  .stat .num { font-family: var(--font-display); font-size: 3rem; font-weight: 800; color: var(--navy); font-variant-numeric: tabular-nums; line-height: 1.1; }
  .stat .num span { background: linear-gradient(120deg, var(--navy), var(--blue) 60%, #19c8ff); -webkit-background-clip: text; background-clip: text; color: transparent; }
  .stat .lbl { color: var(--muted); font-size: 0.9rem; font-weight: 700; letter-spacing: 0.02em; }
  .stat .tick { position: absolute; left: 50%; bottom: 0; transform: translateX(-50%); width: 56px; height: 4px; border-radius: 4px 4px 0 0; background: linear-gradient(90deg, var(--blue), #19c8ff); opacity: 0.85; }

  /* editorial statement panel (wraps the speakable summary) */
  .ai-wrap { position: relative; overflow: hidden; margin-top: 40px; border: 1.5px solid transparent; border-radius: 26px; padding: 34px 40px 36px;
    background: linear-gradient(160deg, #ffffff, #f4f9ff) padding-box, linear-gradient(130deg, rgba(25,123,255,0.5), rgba(25,200,255,0.25) 50%, rgba(0,125,179,0.5)) border-box;
    box-shadow: 0 16px 44px rgba(15,53,103,0.1); }
  .ai-wrap::after { content: ""; position: absolute; right: -90px; top: -90px; width: 260px; height: 260px; border-radius: 50%; background: radial-gradient(circle, rgba(25,200,255,0.18), transparent 70%); pointer-events: none; }
  .ai-label { display: inline-block; text-transform: uppercase; letter-spacing: 2.6px; font-size: 0.72rem; font-weight: 800; margin-bottom: 12px; background: linear-gradient(90deg, var(--blue), #19c8ff); -webkit-background-clip: text; background-clip: text; color: transparent; }
  .ai-wrap #ai-summary { background: transparent; border: none; padding: 0; margin: 0; font-size: 1.14rem; line-height: 1.75; color: #2a3c52; }

  /* icon cards */
  .rounded-section { border-radius: 44px 44px 0 0; }
  .icon-card { display: block; background: linear-gradient(160deg, #ffffff, #f6faff) padding-box, linear-gradient(135deg, rgba(25,123,255,0.45), rgba(25,200,255,0.22) 50%, rgba(0,125,179,0.5)) border-box; border: 1.5px solid transparent; border-radius: 26px; padding: 30px; text-decoration: none; box-shadow: var(--shadow-sm); transition: transform 0.3s cubic-bezier(.2,.8,.2,1), box-shadow 0.3s; }
  .icon-card:hover { transform: translateY(-8px); box-shadow: 0 24px 54px rgba(15,53,103,0.2); }
  .icon-squircle { position: relative; width: 78px; height: 78px; border-radius: 24px; background: linear-gradient(135deg, #eaf3ff, #dff2fa); display: flex; align-items: center; justify-content: center; padding: 16px; box-shadow: inset 0 0 0 1.5px #c9e2f5, 0 8px 20px rgba(15,53,103,0.10); transition: transform 0.3s cubic-bezier(.2,.8,.2,1), box-shadow 0.3s; }
  .icon-card:hover .icon-squircle { transform: scale(1.08) rotate(-4deg); box-shadow: inset 0 0 0 1.5px #9fd0f2, 0 12px 30px rgba(25,123,255,0.35), 0 0 34px rgba(25,200,255,0.35); }
  .icon-squircle :global(svg) { width: 100%; height: 100%; }
  .icon-card h3 { margin: 16px 0 6px; }
  .icon-card p { color: var(--muted); font-size: 0.95rem; }
  .icon-card .more { color: var(--accent); font-weight: 700; }

  /* why panel */
  .why-panel { position: relative; border-radius: 30px; min-height: 420px; background-size: cover; background-position: center; box-shadow: var(--shadow); overflow: visible; }
  .why-photo { position: absolute; inset: 0; border-radius: 30px; background-size: cover; background-position: center; }
  .chip { position: absolute; background: #fff; border-radius: 40px; padding: 10px 18px; font-size: 0.85rem; color: var(--muted); box-shadow: 0 14px 34px rgba(15,53,103,0.18); animation: floaty 6s ease-in-out infinite; }
  .chip b { color: var(--navy); }
  .c1 { top: 24px; left: -18px; }
  .c2 { bottom: 90px; right: -20px; animation-delay: -2s; }
  .c3 { bottom: -16px; left: 40px; animation-delay: -4s; }
  .checklist { list-style: none; margin: 16px 0 0 !important; }
  .checklist li { padding-left: 34px; position: relative; margin: 10px 0; font-weight: 600; color: var(--ink); }
  .checklist li::before { content: "✓"; position: absolute; left: 0; top: 0; width: 24px; height: 24px; border-radius: 50%; background: linear-gradient(135deg, var(--blue), var(--accent)); color: #fff; font-size: 0.8rem; font-weight: 800; display: flex; align-items: center; justify-content: center; }

  /* photo cards */
  .photo-card { position: relative; display: block; border-radius: 28px; overflow: hidden; min-height: 430px; text-decoration: none; box-shadow: var(--shadow); background-size: cover; background-position: center; transition: 0.25s; }
  .photo-card:hover { transform: translateY(-5px); }
  .pc-photo { position: absolute; inset: 0; background-size: cover; background-position: center; transition: transform 0.5s; }
  .photo-card:hover .pc-photo { transform: scale(1.05); }
  .pc-overlay { position: absolute; inset: 0; background: linear-gradient(180deg, rgba(10,34,70,0.0) 25%, rgba(10,34,70,0.9) 80%); }
  .pc-body { position: absolute; left: 0; right: 0; bottom: 0; padding: 30px; color: #fff; }
  .pc-tag { display: inline-block; background: rgba(255,255,255,0.16); border: 1px solid rgba(255,255,255,0.35); backdrop-filter: blur(6px); font-size: 0.7rem; font-weight: 800; letter-spacing: 1.6px; text-transform: uppercase; padding: 6px 14px; border-radius: 30px; margin-bottom: 12px; }
  .pc-body h3 { color: #fff; margin: 0 0 8px; font-size: 1.35rem; }
  .pc-body p { color: #d9e6f7; font-size: 0.95rem; margin: 0 0 14px; }
  .pc-btn { font-weight: 800; color: #66e0ff; }

  /* navy band / cta */
  .navy-band.lux { position: relative; overflow: hidden; border-radius: 30px; }
  .band-glow { position: absolute; width: 420px; height: 420px; border-radius: 50%; background: radial-gradient(circle, rgba(25,200,255,0.28), transparent 65%); top: -160px; right: -120px; animation: drift 14s ease-in-out infinite alternate; }
  .cta-band.lux { border-radius: 30px; }

  /* team photo fallback */

  /* blog thumbs fallback */
  .grad-thumb { background-color: #14396b; background-image: linear-gradient(135deg, #17457e, #007db3); background-size: cover; background-position: center; }

  /* reveals */
  .reveal { opacity: 0; transform: translateY(26px); transition: opacity 0.7s ease, transform 0.7s ease; }
  .reveal.in { opacity: 1; transform: none; }

  /* ===== v8.4: editorial blog rows ===== */
  .b8-list { display: flex; flex-direction: column; gap: 44px; max-width: 1000px; margin: 10px auto 0; }
  .b8-row { display: grid; grid-template-columns: 1fr 1fr; gap: clamp(24px, 4vw, 52px); align-items: center; }
  .b8-tags { display: flex; gap: 18px; text-transform: uppercase; letter-spacing: 2px; font-size: 0.72rem; font-weight: 800; color: var(--muted); margin-bottom: 12px; }
  .b8-tags span:first-child { background: linear-gradient(90deg, var(--blue), #19c8ff); -webkit-background-clip: text; background-clip: text; color: transparent; }
  .b8-copy h3 { margin: 0; font-size: clamp(1.25rem, 2.4vw, 1.75rem); line-height: 1.2; }
  .b8-copy h3 a { color: var(--navy); text-decoration: none; background-image: linear-gradient(90deg, var(--blue), #19c8ff); background-size: 0% 2px; background-repeat: no-repeat; background-position: 0 100%; transition: background-size 0.35s ease; padding-bottom: 3px; }
  .b8-copy h3 a:hover { background-size: 100% 2px; }
  .b8-copy p { color: var(--muted); margin: 12px 0 0; font-size: 0.97rem; line-height: 1.65; }
  .b8-meta { margin-top: 16px; font-size: 0.86rem; color: var(--muted); }
  .b8-meta i { font-style: normal; margin: 0 6px; color: rgba(15, 53, 103, 0.3); }
  .b8-more { display: inline-flex; align-items: center; gap: 8px; margin-top: 16px; font-weight: 800; color: var(--blue); text-decoration: none; }
  .b8-arrow { transition: transform 0.25s cubic-bezier(0.2, 0.7, 0.2, 1); }
  .b8-more:hover .b8-arrow { transform: translateX(5px); }
  .b8-media { display: block; aspect-ratio: 16 / 9; overflow: hidden; border-radius: 18px; border: 1.5px solid var(--line); box-shadow: var(--shadow-sm); }
  .b8-media img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.5s cubic-bezier(0.2, 0.7, 0.2, 1), opacity 0.3s ease; }
  .b8-media:hover img { transform: scale(1.05); opacity: 0.85; }
  @media (max-width: 760px) {
    .b8-row { grid-template-columns: 1fr; gap: 16px; }
    .b8-media { order: -1; }
  }



  /* ===== v8.2: fanned team deck ===== */
  .team-fan { position: relative; height: 300px; margin-top: 26px; }
  .fan-card { position: absolute; left: 50%; top: 50%; width: clamp(120px, 13vw, 172px); height: clamp(120px, 13vw, 172px); border-radius: 18px; overflow: visible; opacity: 0;
    transform: translate(-50%, -50%) scale(0.4); transition: transform 0.8s cubic-bezier(0.34, 1.56, 0.64, 1), opacity 0.5s ease; transition-delay: var(--fd); text-decoration: none; }
  .team-fan.in .fan-card { opacity: 1; transform: translate(calc(-50% + var(--fx)), calc(-50% + var(--fy))) rotate(var(--fr)); }
  .fan-card img { width: 100%; height: 100%; object-fit: cover; border-radius: 18px; border: 3px solid #fff; box-shadow: 0 18px 44px rgba(15, 53, 103, 0.28); display: block; transition: box-shadow 0.25s ease; }
  .fan-card:hover { z-index: 99 !important; }
  .team-fan.in .fan-card:hover { transform: translate(calc(-50% + var(--fx)), calc(-50% + var(--fy) - 10px)) rotate(0deg) scale(1.12); transition-delay: 0s; transition-duration: 0.35s; }
  .fan-card:hover img { box-shadow: 0 28px 60px rgba(15, 53, 103, 0.4), 0 0 0 3px rgba(25, 123, 255, 0.35); }
  .fan-name { position: absolute; left: 50%; bottom: -14px; transform: translateX(-50%) translateY(6px); display: flex; flex-direction: column; align-items: center; gap: 0; background: #fff; border: 1.5px solid var(--line); border-radius: 12px; padding: 6px 14px; font-size: 0.72rem; color: var(--muted); white-space: nowrap; box-shadow: var(--shadow-sm); opacity: 0; transition: opacity 0.22s ease, transform 0.22s ease; pointer-events: none; }
  .fan-name b { color: var(--navy); font-size: 0.88rem; }
  .fan-card:hover .fan-name { opacity: 1; transform: translateX(-50%) translateY(0); }
  @media (max-width: 700px) {
    .team-fan { height: 210px; }
    .fan-card { width: clamp(84px, 22vw, 110px); height: clamp(84px, 22vw, 110px); }
    .team-fan.in .fan-card { transform: translate(calc(-50% + var(--fx) * 0.42), calc(-50% + var(--fy) * 0.6)) rotate(var(--fr)); }
    .team-fan.in .fan-card:hover { transform: translate(calc(-50% + var(--fx) * 0.42), calc(-50% + var(--fy) * 0.6)) scale(1.1); }
  }
  @media (prefers-reduced-motion: reduce) {
    .fan-card, .team-fan.in .fan-card { transition: none; }
  }

  /* ===== v8.1: sticky stacking coverage deck ===== */
  .stackzone { position: relative; background: linear-gradient(160deg, #081d3d 0%, var(--navy) 50%, #0d2a52 100%); border-radius: 34px; margin: 40px 14px; padding: 30px 0 60px; overflow: clip; }
  .sz-gridbg { position: absolute; inset: 0; background: linear-gradient(to right, rgba(142, 201, 255, 0.07) 1px, transparent 1px), linear-gradient(to bottom, rgba(142, 201, 255, 0.07) 1px, transparent 1px); background-size: 54px 54px; -webkit-mask-image: radial-gradient(ellipse 70% 60% at 50% 0%, #000 60%, transparent 100%); mask-image: radial-gradient(ellipse 70% 60% at 50% 0%, #000 60%, transparent 100%); }
  .sz-glow { position: absolute; width: 50vw; height: 50vw; top: -20vw; right: -14vw; border-radius: 50%; background: radial-gradient(circle, rgba(25, 200, 255, 0.13), transparent 62%); filter: blur(70px); }
  .sz-cols { position: relative; z-index: 2; display: grid; grid-template-columns: 0.9fr 1.1fr; gap: clamp(30px, 5vw, 80px); align-items: stretch; }
  .sz-left { position: relative; }
  .sz-pinhead { position: sticky; top: 120px; padding: 40px 0; }
  .sz-pinhead h2 { color: #fff; font-size: clamp(1.9rem, 3.6vw, 2.9rem); margin: 10px 0 14px; line-height: 1.08; }
  .sz-pinhead p { color: #b9d4f0; max-width: 420px; font-size: 1.02rem; line-height: 1.7; }
  .sz-pin { position: sticky; top: 100px; height: 74vh; display: grid; place-content: center; margin: 0; }
  .sz-card { display: block; width: min(480px, 88%); margin: 0 auto; background: rgba(255, 255, 255, 0.97); border: 1.5px solid rgba(255, 255, 255, 0.9); border-radius: 22px; padding: 30px 32px 26px; text-decoration: none; box-shadow: 0 34px 80px rgba(3, 12, 28, 0.5); transform: rotate(0deg) translateY(calc(var(--i) * 6px)); transition: transform 0.4s cubic-bezier(0.2, 0.7, 0.2, 1), box-shadow 0.3s ease; }
  .sz-card.r0 { transform: rotate(2.6deg) translateY(calc(var(--i) * 6px)); }
  .sz-card.r2 { transform: rotate(-2.6deg) translateY(calc(var(--i) * 6px)); }
  .sz-card:hover { transform: rotate(0deg) translateY(calc(var(--i) * 6px - 6px)); box-shadow: 0 44px 96px rgba(3, 12, 28, 0.6); }
  .sz-top { display: flex; align-items: center; justify-content: space-between; margin-bottom: 14px; }
  .sz-idx { font-family: var(--font-display); font-weight: 800; font-size: 2rem; background: linear-gradient(120deg, var(--blue), #19c8ff); -webkit-background-clip: text; background-clip: text; color: transparent; opacity: 0.85; }
  .sz-card h3 { color: var(--navy); font-size: 1.35rem; margin: 0 0 8px; }
  .sz-card p { color: var(--muted); font-size: 0.95rem; line-height: 1.65; margin: 0 0 14px; }
  .sz-more { color: var(--blue); font-weight: 800; font-size: 0.95rem; }
  @media (max-width: 900px) {
    .sz-cols { grid-template-columns: 1fr; }
    .sz-pinhead { position: static; padding: 30px 0 6px; text-align: center; }
    .sz-pinhead p { margin: 0 auto; }
    .sz-pin { position: static; height: auto; margin: 14px 0; }
    .sz-card, .sz-card.r0, .sz-card.r2 { transform: none; width: 100%; }
  }
  @media (prefers-reduced-motion: reduce) {
    .sz-pin { position: static; height: auto; margin: 14px 0; }
    .sz-card, .sz-card.r0, .sz-card.r2 { transform: none; }
  }

  /* ===== v8.3: method coverflow carousel ===== */
  .mframe { max-width: 1060px; margin: 0 auto; }
  .mpanel { position: relative; }
  .mbadge { display: flex; width: fit-content; margin: 0 auto; align-items: center; gap: 7px; border: 1.5px solid rgba(15, 53, 103, 0.12); background: #fff; border-radius: 999px; padding: 7px 16px; font-weight: 700; font-size: 0.85rem; color: var(--navy); box-shadow: var(--shadow-sm); }
  .mspark { color: var(--gold); }
  .mhead { text-align: center; padding: 16px 16px 6px; }
  .mhead h2 { margin: 0 0 4px; }
  .mhead p { color: var(--muted); margin: 0; }
  .mcarousel { position: relative; padding: 22px 0 10px; }
  .mtrack { position: relative; height: 460px; perspective: 1100px; }
  .mcard { position: absolute; left: 50%; top: 8px; width: min(300px, 78vw); margin-left: max(-150px, -39vw); background: linear-gradient(165deg, #0f3567, #17457e); border-radius: 20px; overflow: hidden; box-shadow: 0 24px 60px rgba(15, 53, 103, 0.3); transition: transform 0.65s cubic-bezier(0.2, 0.7, 0.2, 1), opacity 0.5s ease; will-change: transform; }
  .mcard-img { aspect-ratio: 1 / 1; max-height: 240px; width: 100%; overflow: hidden; }
  .mcard-img img { width: 100%; height: 100%; object-fit: cover; display: block; }
  .mcard-body { padding: 16px 20px 20px; color: #fff; position: relative; }
  .mnum { position: absolute; right: 16px; top: 8px; font-family: var(--font-display); font-weight: 800; font-size: 2rem; background: linear-gradient(120deg, #19c8ff, #f5b942); -webkit-background-clip: text; background-clip: text; color: transparent; opacity: 0.9; }
  .mcard-body h3 { color: #fff; margin: 0 0 6px; font-size: 1.3rem; }
  .mcard-body p { color: #c9def5; font-size: 0.88rem; line-height: 1.55; margin: 0 0 10px; }
  .mlink { color: #66e0ff; font-weight: 800; font-size: 0.9rem; text-decoration: none; }
  .mlink:hover { text-decoration: underline; }
  .mnav { position: absolute; top: 45%; z-index: 5; width: 44px; height: 44px; border-radius: 50%; border: 1.5px solid rgba(15, 53, 103, 0.12); background: #fff; color: var(--navy); font-size: 1.1rem; font-weight: 800; cursor: pointer; box-shadow: var(--shadow-sm); transition: transform 0.2s ease, box-shadow 0.2s ease; }
  .mnav:hover { transform: scale(1.08); box-shadow: var(--shadow); }
  .mprev { left: 10px; } .mnext { right: 10px; }
  .mdots { display: flex; justify-content: center; gap: 8px; padding: 14px 0 10px; }
  .mdots button { width: 9px; height: 9px; border-radius: 50%; border: none; background: rgba(15, 53, 103, 0.18); cursor: pointer; transition: transform 0.2s, background 0.2s; padding: 0; }
  .mdots button.on { background: linear-gradient(120deg, var(--blue), #19c8ff); transform: scale(1.35); }
  @media (max-width: 640px) {
    .mtrack { height: 440px; }
    .mnav { display: none; }
  }


  /* ===== v5: scroll progress bar ===== */
  #scrollbar { position: fixed; top: 0; left: 0; right: 0; height: 3px; z-index: 90; background: transparent; }
  #scrollbar i { display: block; height: 100%; width: 0%; background: linear-gradient(90deg, var(--blue), #19c8ff, var(--accent)); box-shadow: 0 0 12px rgba(25,200,255,.7); }

  /* ===== v5: cinematic hero background ===== */
  .hero-bg { position: absolute; inset: -6%; background-size: cover; background-position: center 30%; animation: kenburns 26s ease-in-out infinite alternate; will-change: transform; }
  @keyframes kenburns { from { transform: scale(1) translateY(0); } to { transform: scale(1.12) translateY(-2.5%); } }
  .hero-duotone { position: absolute; inset: 0; background: linear-gradient(100deg, rgba(7,24,50,0.96) 0%, rgba(10,34,70,0.86) 38%, rgba(15,53,103,0.55) 68%, rgba(15,53,103,0.35) 100%); }
  .aurora { position: absolute; inset: -30%; background: conic-gradient(from 180deg at 50% 50%, transparent 0deg, rgba(25,123,255,0.22) 70deg, rgba(25,200,255,0.28) 140deg, transparent 200deg, rgba(0,125,179,0.2) 290deg, transparent 360deg); filter: blur(60px); animation: auroraSpin 22s linear infinite; mix-blend-mode: screen; }
  @keyframes auroraSpin { to { transform: rotate(360deg); } }

  /* ===== v5: conic ring behind glass card ===== */
  .glass-card::before { content: ""; position: absolute; inset: -2px; border-radius: 24px; padding: 2px; background: conic-gradient(from var(--sweep,0deg), transparent 0%, #19c8ff 12%, transparent 30%, transparent 70%, #197bff 82%, transparent 100%); -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0); -webkit-mask-composite: xor; mask-composite: exclude; animation: ringSweep 6s linear infinite; pointer-events: none; }
  @property --sweep { syntax: '<angle>'; initial-value: 0deg; inherits: false; }
  @keyframes ringSweep { to { --sweep: 360deg; } }

  /* ===== v5: shine sweep on cards ===== */
  .icon-card, .img-card, .photo-card { position: relative; overflow: hidden; }
  .icon-card::after, .img-card::after { content: ""; position: absolute; top: 0; bottom: 0; width: 55%; left: -80%; background: linear-gradient(105deg, transparent, rgba(255,255,255,0.5), transparent); transform: skewX(-18deg); transition: left 0.65s ease; pointer-events: none; }
  .icon-card:hover::after, .img-card:hover::after { left: 130%; }
  .photo-card::after { content: ""; position: absolute; top: 0; bottom: 0; width: 55%; left: -80%; background: linear-gradient(105deg, transparent, rgba(255,255,255,0.22), transparent); transform: skewX(-18deg); transition: left 0.7s ease; pointer-events: none; z-index: 3; }
  .photo-card:hover::after { left: 130%; }

  /* ===== v5: stat tilt ===== */
  .stat { transition: transform 0.25s ease, box-shadow 0.25s ease; }
  .stat:hover { transform: translateY(-5px) rotate(-1deg) scale(1.02); box-shadow: 0 16px 40px rgba(15,53,103,0.16); }

  /* ===== v5: hand-drawn underline ===== */
  .uline { position: relative; display: inline-block; }
  .uline svg { position: absolute; left: 0; right: 0; bottom: -0.28em; width: 100%; height: 0.32em; }
  .uline svg path { stroke-dasharray: 240; stroke-dashoffset: 240; }
  .reveal.in ~ * .uline svg path, .uline.draw svg path { animation: drawLine 1.1s ease 0.3s forwards; }
  @keyframes drawLine { to { stroke-dashoffset: 0; } }

  /* ===== Cinematic intro overlay ===== */
  #site-intro { position: fixed; inset: 0; z-index: 99999; background: #000; display: flex; align-items: center; justify-content: center; opacity: 1; transition: opacity 0.95s ease; }
  #site-intro video { position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; }
  #site-intro .intro-brand { position: absolute; bottom: 9vh; left: 0; right: 0; text-align: center; color: rgba(255,255,255,0.85); font-family: var(--font-display); font-weight: 700; font-size: clamp(0.7rem, 1.4vw, 0.95rem); letter-spacing: 0.6em; text-indent: 0.6em; animation: introBrand 2.4s ease 0.6s both; pointer-events: none; }
  @keyframes introBrand { from { opacity: 0; transform: translateY(12px); letter-spacing: 0.9em; } to { opacity: 1; transform: translateY(0); letter-spacing: 0.6em; } }
  #intro-skip { position: absolute; right: 26px; bottom: 26px; background: rgba(255,255,255,0.08); color: rgba(255,255,255,0.85); border: 1px solid rgba(255,255,255,0.25); border-radius: 999px; padding: 9px 18px; font-size: 0.82rem; font-weight: 700; cursor: pointer; backdrop-filter: blur(8px); transition: background 0.25s, color 0.25s; }
  #intro-skip:hover { background: rgba(255,255,255,0.2); color: #fff; }
  #site-intro.intro-out { opacity: 0; pointer-events: none; }
  html.intro-lock, html.intro-lock body { overflow: hidden; }

  /* ===== v5: review showcase ===== */
  .review-showcase { position: relative; display: grid; grid-template-columns: auto 1fr auto; align-items: center; gap: 34px; background: linear-gradient(120deg, #0a2246, var(--navy) 55%, #17457e); border-radius: 28px; padding: 40px 46px; text-decoration: none; color: #fff; overflow: hidden; box-shadow: var(--shadow); transition: transform 0.25s; }
  .review-showcase:hover { transform: translateY(-4px); }
  .rs-glow { position: absolute; width: 420px; height: 420px; border-radius: 50%; right: -140px; top: -180px; background: radial-gradient(circle, rgba(25,200,255,0.3), transparent 65%); animation: drift 12s ease-in-out infinite alternate; }
  .rs-left { display: flex; align-items: center; gap: 22px; position: relative; }
  .rs-g { width: 74px; height: 74px; border-radius: 20px; background: #fff; color: #4285f4; font-family: var(--font-display); font-weight: 800; font-size: 2.4rem; display: flex; align-items: center; justify-content: center; box-shadow: 0 12px 30px rgba(0,0,0,0.3); }
  .rs-stars { font-size: 1.1rem; }
  .rs-big { font-family: var(--font-display); font-size: 2.6rem; font-weight: 800; line-height: 1; }
  .rs-big em { font-style: normal; font-size: 1.1rem; color: #8ec9ff; font-weight: 600; }
  .rs-count { font-family: var(--font-display); font-size: 1.4rem; font-weight: 700; }
  .rs-sub { color: #b9cbe1; font-size: 0.95rem; margin-top: 4px; max-width: 480px; }
  .rs-cta { position: relative; font-weight: 800; color: #66e0ff; white-space: nowrap; }
  @media (max-width: 860px) { .review-showcase { grid-template-columns: 1fr; gap: 18px; text-align: left; } }

  /* ===== Real Google reviews wall ===== */
  .review-wall { margin-top: 34px; overflow: hidden; }
  .rv-row { overflow: hidden; padding: 10px 0; -webkit-mask-image: linear-gradient(90deg, transparent, #000 6%, #000 94%, transparent); mask-image: linear-gradient(90deg, transparent, #000 6%, #000 94%, transparent); }
  .rv-track { display: flex; gap: 16px; width: max-content; animation: rvScroll 55s linear infinite; }
  .rv-reverse .rv-track { animation-direction: reverse; }
  .rv-row:hover .rv-track { animation-play-state: paused; }
  @keyframes rvScroll { from { transform: translateX(0); } to { transform: translateX(-50%); } }
  .rv-card { display: flex; flex-direction: column; gap: 10px; width: 272px; flex-shrink: 0; background: rgba(255, 255, 255, 0.85); border: 1.5px solid rgba(25, 123, 255, 0.14); border-radius: 16px; padding: 16px 18px; text-decoration: none; color: var(--ink, #1c2b3a); box-shadow: 0 6px 18px rgba(15, 53, 103, 0.06); backdrop-filter: blur(8px); transition: transform 0.25s ease, box-shadow 0.25s ease, border-color 0.25s ease; }
  .rv-card:hover { transform: translateY(-3px); border-color: rgba(25, 123, 255, 0.4); box-shadow: 0 14px 34px rgba(15, 53, 103, 0.14); }
  .rv-head { display: flex; align-items: center; gap: 10px; }
  .rv-stars { color: #f5b942; letter-spacing: 1px; font-size: 0.7rem; }
  .rv-g { margin-left: auto; font-weight: 800; font-family: var(--font-display); font-size: 1.05rem; background: conic-gradient(from -45deg, #4285F4, #34A853, #FBBC05, #EA4335, #4285F4); -webkit-background-clip: text; background-clip: text; color: transparent; }
  .rv-text { margin: 0; font-size: 0.87rem; line-height: 1.55; color: #2a3c52; display: -webkit-box; -webkit-line-clamp: 3; -webkit-box-orient: vertical; overflow: hidden; }
  .rv-avatar { position: relative; width: 34px; height: 34px; border-radius: 50%; display: grid; place-items: center; color: #fff; font-weight: 800; font-size: 0.85rem; background: linear-gradient(135deg, var(--blue), var(--accent)); box-shadow: 0 4px 12px rgba(25, 123, 255, 0.35); flex-shrink: 0; }
  .rv-avatar img { position: absolute; inset: 0; width: 100%; height: 100%; border-radius: 50%; object-fit: cover; border: 2px solid rgba(255, 255, 255, 0.9); }
  .rv-meta { display: flex; flex-direction: column; line-height: 1.3; min-width: 0; }
  .rv-meta b { font-size: 0.88rem; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
  .rv-meta em { font-style: normal; font-size: 0.74rem; color: var(--muted, #5b6b7e); }
  .rv-note { text-align: center; font-size: 0.85rem; color: var(--muted, #5b6b7e); margin: 16px 0 0; }
  .rv-note a { color: var(--blue); font-weight: 700; }
  @media (max-width: 640px) { .rv-card { width: 240px; } }
  @media (prefers-reduced-motion: reduce) { .rv-track { animation: none; } .rv-row { overflow-x: auto; } }

  @media (max-width: 900px) {
    .lux-hero-grid { grid-template-columns: 1fr; gap: 44px; }
    .lux-hero { padding: 56px 0 64px; border-radius: 22px; margin: 6px 8px 0; }
    .hero-panel { min-height: 380px; }
    .glass-card { left: 12px; }
    .float-pill { right: 8px; }
    .photo-card { min-height: 360px; }
    .rounded-section { border-radius: 28px 28px 0 0; }
    .chip.c1 { left: 8px; } .chip.c2 { right: 8px; }
  }
  @media (prefers-reduced-motion: reduce) {
    .mesh, .btn.glow, .float-pill, .marquee-track, .band-glow, .glass-card, .chip, .hero-bg, .aurora, .glass-card::before, .rs-glow { animation: none !important; }
    .uline svg path { stroke-dashoffset: 0; }
    .hw, .grad-anim { animation: none !important; opacity: 1; transform: none; }
    .reveal { opacity: 1; transform: none; transition: none; }
  }
  /* mobile performance tier (homepage decor) — keep last so it wins the cascade */
  @media (pointer: coarse), (max-width: 1023px) {
    .mesh, .aurora, .rs-glow, .band-glow, .hero-bg, .grad-anim, .hs-grad, .glass-card, .float-pill, .chip { animation: none !important; }
    .rv-card { backdrop-filter: none !important; -webkit-backdrop-filter: none !important; background: #fff !important; }
    .glass-card { backdrop-filter: none !important; -webkit-backdrop-filter: none !important; background: rgba(10, 34, 70, 0.78) !important; }
    .kicker-chip, .lp-kick, .hs-panel { backdrop-filter: none !important; -webkit-backdrop-filter: none !important; }
  }
</style>

<script>
  const reduce = matchMedia('(prefers-reduced-motion: reduce)').matches;
  const els = document.querySelectorAll('.reveal');
  if (reduce) { els.forEach((e) => e.classList.add('in')); }
  else {
    const io = new IntersectionObserver((entries) => {
      entries.forEach((en) => { if (en.isIntersecting) { en.target.classList.add('in'); io.unobserve(en.target); } });
    }, { threshold: 0.15 });
    els.forEach((e) => io.observe(e));
  }
  const counters = document.querySelectorAll('[data-count]');
  const runCount = (el) => {
    const target = parseFloat(el.dataset.count || '0');
    const dec = parseInt(el.dataset.dec || '0');
    if (reduce || target === 0) { el.textContent = target.toFixed(dec); return; }
    const t0 = performance.now(); const dur = 1400;
    const step = (t) => {
      const p = Math.min(1, (t - t0) / dur);
      el.textContent = (target * (1 - Math.pow(1 - p, 3))).toFixed(dec);
      if (p < 1) requestAnimationFrame(step);
    };
    requestAnimationFrame(step);
  };
  const cio = new IntersectionObserver((entries) => {
    entries.forEach((en) => { if (en.isIntersecting) { runCount(en.target); cio.unobserve(en.target); } });
  }, { threshold: 0.4 });
  counters.forEach((c) => cio.observe(c));

  // v8.3: method coverflow carousel
  (() => {
    const track = document.getElementById('mTrack');
    if (!track) return;
    const cards = [...track.querySelectorAll('.mcard')];
    const dotsWrap = document.getElementById('mDots');
    const N = cards.length; // 8 (4 steps x2)
    const STEPS = 4;
    let active = 0;
    let timer = null;
    const spacing = Math.min(250, innerWidth * 0.28);
    // dots represent the 4 steps
    for (let i = 0; i < STEPS; i++) {
      const b = document.createElement('button');
      b.setAttribute('role', 'tab');
      b.setAttribute('aria-label', 'Go to step ' + (i + 1));
      b.addEventListener('click', () => { goTo(i); restart(); });
      dotsWrap.appendChild(b);
    }
    const dots = [...dotsWrap.children];
    const render = () => {
      cards.forEach((c, i) => {
        let o = ((i - active) % N + N) % N;
        if (o > N / 2) o -= N;
        const abs = Math.abs(o);
        const visible = abs <= 2;
        c.style.opacity = visible ? String(1 - abs * 0.18) : '0';
        c.style.pointerEvents = o === 0 ? 'auto' : visible ? 'auto' : 'none';
        c.style.zIndex = String(20 - abs);
        c.style.transform = `translateX(${o * spacing}px) translateZ(${-abs * 120}px) rotateY(${o * -7}deg) scale(${1 - abs * 0.1})`;
        c.setAttribute('aria-hidden', o === 0 ? 'false' : 'true');
      });
      dots.forEach((d, i) => d.classList.toggle('on', i === active % STEPS));
    };
    const goTo = (i) => { active = ((i % N) + N) % N; render(); };
    const next = () => goTo(active + 1);
    const prev = () => goTo(active - 1);
    document.querySelector('.mnext').addEventListener('click', () => { next(); restart(); });
    document.querySelector('.mprev').addEventListener('click', () => { prev(); restart(); });
    // click a side card to bring it to center
    cards.forEach((c, i) => c.addEventListener('click', (e) => {
      let o = ((i - active) % N + N) % N; if (o > N / 2) o -= N;
      if (o !== 0) { e.preventDefault(); goTo(i); restart(); }
    }));
    // swipe
    let sx = null;
    track.addEventListener('pointerdown', (e) => { sx = e.clientX; }, { passive: true });
    track.addEventListener('pointerup', (e) => {
      if (sx === null) return;
      const dx = e.clientX - sx; sx = null;
      if (Math.abs(dx) > 40) { dx < 0 ? next() : prev(); restart(); }
    }, { passive: true });
    // autoplay (pause on hover, off for reduced motion)
    const start = () => { if (!reduce) timer = setInterval(next, 3200); };
    const stop = () => { if (timer) { clearInterval(timer); timer = null; } };
    const restart = () => { stop(); start(); };
    const wrap = document.getElementById('mCarousel');
    wrap.addEventListener('mouseenter', stop);
    wrap.addEventListener('mouseleave', start);
    addEventListener('resize', render, { passive: true });
    render();
    start();
  })();

  // v5: scroll progress bar + parallax (skipped under reduced motion)
  const fill = document.getElementById('scrollbar-fill');
  const heroBg = document.getElementById('heroBg');
  const pcs = [...document.querySelectorAll('.pc-photo')];
  if (!reduce) {
    let ticking = false;
    const onScroll = () => {
      if (ticking) return;
      ticking = true;
      requestAnimationFrame(() => {
        const max = document.documentElement.scrollHeight - innerHeight;
        if (fill) fill.style.width = (max > 0 ? (scrollY / max) * 100 : 0) + '%';
        // heroBg motion handled by the Ken Burns CSS animation
        pcs.forEach((el) => {
          const r = el.parentElement.getBoundingClientRect();
          if (r.top < innerHeight && r.bottom > 0) {
            const p = (r.top + r.height / 2 - innerHeight / 2) / innerHeight;
            el.style.transform = `scale(1.08) translateY(${p * -22}px)`;
          }
        });
        ticking = false;
      });
    };
    addEventListener('scroll', onScroll, { passive: true });
    onScroll();
  }
  // underline draw trigger
  const ul = document.querySelector('.uline');
  if (ul) {
    const uio = new IntersectionObserver((es) => { es.forEach((e) => { if (e.isIntersecting) { ul.classList.add('draw'); uio.disconnect(); } }); }, { threshold: 0.6 });
    uio.observe(ul);
  }

  // ===== Cinematic intro: play once per session, then dissolve into the page =====
  (() => {
    const intro = document.getElementById('site-intro');
    if (!intro) return;
    const vid = document.getElementById('intro-video');
    let seen = false;
    try { seen = sessionStorage.getItem('ph_intro_seen') === '1'; } catch (e) {}
    const saveData = navigator.connection && navigator.connection.saveData;
    const mobile = matchMedia('(max-width: 820px)').matches;
    if (seen || reduce || saveData || mobile) { intro.remove(); return; }
    vid.src = vid.dataset.src;
    try { sessionStorage.setItem('ph_intro_seen', '1'); } catch (e) {}

    document.documentElement.classList.add('intro-lock');
    let done = false;
    const end = () => {
      if (done) return;
      done = true;
      intro.classList.add('intro-out');
      document.documentElement.classList.remove('intro-lock');
      setTimeout(() => intro.remove(), 1000);
    };
    vid.addEventListener('ended', end);
    vid.addEventListener('error', end);
    document.getElementById('intro-skip').addEventListener('click', end);
    // Safety net: never hold the page hostage
    setTimeout(end, 9000);
    const p = vid.play();
    if (p && p.catch) p.catch(end);
  })();
</script>
